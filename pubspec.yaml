name: surveyincome
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  floating_search_bar: ^0.3.0
  smooth_page_indicator: ^0.1.5
  flutter_local_notifications: ^1.4.1
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/

  fonts:
    - family: BoyarskyMon
      fonts:
        - asset: assets/fonts/BoyarskyMon.ttf
